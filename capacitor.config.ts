import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devfath.ncapp',
  appName: 'next-capacitor',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
